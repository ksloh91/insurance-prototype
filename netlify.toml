[build]
  command = "npm run generate"
  publish = "dist"

# Enable Identity + Git Gateway in Netlify dashboard for /admin CMS access

[[redirects]]
  from = "/admin/*"
  to = "/admin/index.html"
  status = 200
